/**
 * Checks to see if the eth-local directory exist.
 * @returns {boolean}
 */
export declare function verify(): boolean;
/**
 * Creates the eth-local directory in the users root.
 * @returns {boolean}
 */
export declare function setup(): boolean;
